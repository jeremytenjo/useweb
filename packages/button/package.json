{
  "name": "@useweb/button",
  "version": "0.0.6",
  "main": "./build/index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "node ./node_modules/@useweb/compiler/build --jsx",
    "build:watch": "node ./node_modules/@useweb/compiler/build --jsx --watch",
    "prepublish": "npm run build",
    "deploy": "npm run build && npm publish --access public"
  },
  "devDependencies": {
    "esbuild": "^0.12.26"
  },
  "peerDependencies": {
    "react": "^17.0.0"
  },
  "dependencies": {
    "@useweb/compiler": "0.0.3",
    "@useweb/use-event-listener": "0.0.2"
  }
}
