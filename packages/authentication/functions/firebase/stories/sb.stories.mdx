import { Meta } from '@storybook/addon-docs'

<Meta title='Lib/Authentication/Firebase' />

# Continue with Social account

Log in with a social media account.

Providers: Google, Facebook

Defaults: `provider = 'google'`

Returns `user` or `error`

# authSocial

```js
import authSocial from @useweb/lib/authentication/Functions/Firebase/auth.social
```

```js
res = await authSocial({ provider: 'google' })
```

# Continue with Email

Login or Register with a email.

Defaults: `action = 'login'` | `email = ''` | `register = ''`

Returns `user` or `error`

# authEmail

```js
import authEmail from @useweb/lib/authentication/Functions/Firebase/auth.email
```

```js
const { user, error } = await authEmail({
  action: 'login',
  email: 'test@gmail',
  password: 'password1246',
})
```

# Check

Check if the user is signed in. Returns `user object` or `false`

# authCheck

```js
import authCheck from @useweb/lib/authentication/Functions/Firebase/auth.check
```

```js
res = await firebaseAuth()
```
