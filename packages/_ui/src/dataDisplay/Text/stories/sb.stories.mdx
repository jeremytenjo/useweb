import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import B from '../../../storybook/variationBlock/index'
import Text from '../'

<Meta
  title='Data display/Text'
  component={Text}
  argTypes={{
    variant: {
      control: {
        type: 'select',
        options: ['h1', 'h2'],
      },
    },
    text: { control: 'text' },
  }}
/>

# Text

```js
<Text tag='h4' variant='h3' text='Header 3' />
```

export const Template = (args) => <Text {...args} />

<Story
  name='Dev'
  args={{
    variant: 'h1',
    text: 'This is text',
  }}
>
  {Template.bind({})}
</Story>

#### Styling

`variant` h3 will apply class .typography-h3 {}

<Canvas>
  <Story name='variations'>
    <>
      <B background>
        <div style={{ display: 'grid', gridGap: '20px' }}>
          <Text variant='h1' text='Header 1' />
          <Text variant='h2' text='Header 2' />
          <Text variant='h3' tag='h4' text='Header 3' />
          <Text variant='h4' text='Header 4' />
          <Text variant='h5' text='Header 5' />
          <Text variant='h6' text='Header 6' />
          <Text variant='subtitle1' text='Subtitle 1' />
          <Text variant='subtitle2' text='Subtitle 2' />
          <Text variant='body1' text='Body 1' />
          <Text variant='body2' text='Body 2' />
          <Text variant='button' text='Button' />
          <Text variant='caption' text='Caption' />
          <Text variant='overline' text='Overline' />
        </div>
      </B>
    </>
  </Story>
</Canvas>

#### Classes examples

```css
 .typography-h1 {
    font-family:  var(--font-primary);
    font-style: normal;
    font-weight: 300;
    font-size: 96px;
    letter-spacing: -1.5px;
    color: var(--color-black)
  }

  .typography-h2 {
    font-family:  var(--font-primary);
    font-style: normal;
    font-weight: 300;
    font-size: 60px;
    letter-spacing: -0.5px;
    color: var(--color-black)
  }

  .typography-body1 {
    font-family:  var(--font-primary);
    font-style: normal;
    font-weight: normal;
    font-size: 18px;
    letter-spacing: 0.5px;
    color: var(--color-black)
  }

  .typography-subtitle1 {
    font-family:  var(--font-primary);
  font-style: normal;
  font-weight: normal;
  font-size: 16px;
  letter-spacing: 0.15px;
  color: var(--color-black)
  }

  .typography-button {
    font-family:  var(--font-primary);
    font-style: normal;
    font-weight: bold;
    font-size: 12px;
    letter-spacing: 0.75px;
    text-transform: uppercase;
    color: var(--color-black)
  }
  .typography-caption {
    font-family:  var(--font-primary);
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  letter-spacing: 0.4px;
  color: var(--color-black)
  }
  .typography-overline {
    font-family:  var(--font-primary);
    font-style: normal;
    font-weight: 500;
    font-size: 10px;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--color-black)
  }
}
```
