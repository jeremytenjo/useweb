import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Example from './example'

<Meta title='Lib/Input/UseClickOutside' />

# useClickOutside

```js
import useClickOutside from '@useweb/lib/input/useClickOutside'
```

## Example

<Story name='Example'>
  <Example />
</Story>

```js
import Box from '../../../dataDisplay/Box'
import useClickOutside from '../'

export default () => {
  const boxRef = useRef(null)

  const onClickOutside = () => {
    alert('clicked outside green box')
  }

  useClickOutside(boxRef, onClickOutside)

  return (
    <Box ref={boxRef} styles={boxStyles}>
      Click outside this box
    </Box>
  )
}
```
